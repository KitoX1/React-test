export const PATHS: IPATHS = {
    addRestaurant: '/add_restaurant',
    auth: '/auth',
    restaurants: '/restaurants'
}

interface IPATHS {
    addRestaurant: string,
    auth: string,
    restaurants: string
}